version: "3"

services:
  namenode:
    image: bataeves/hadoop-namenode:2.0.0-hadoop2.10.1-java8
    container_name: namenode
    restart: always
    ports:
      - 50070:50070
      - 9000:9000
    volumes:
      - hadoop_namenode:/hadoop/dfs/name
    environment:
      - CLUSTER_NAME=test
    env_file:
      - ./hadoop.env

  datanode:
    image: bataeves/hadoop-datanode:2.0.0-hadoop2.10.1-java8
    container_name: datanode
    restart: always
    volumes:
      - hadoop_datanode:/hadoop/dfs/data
    environment:
      SERVICE_PRECONDITION: "namenode:50070"
    env_file:
      - ./hadoop.env
  
  resourcemanager:
    image: bataeves/hadoop-resourcemanager:2.0.0-hadoop2.10.1-java8
    container_name: resourcemanager
    restart: always
    environment:
      SERVICE_PRECONDITION: "namenode:9000 namenode:50070 datanode:50075"
    env_file:
      - ./hadoop.env
    ports:
      - 8088:8088
      - 8032

  nodemanager1:
    image: bataeves/hadoop-nodemanager:2.0.0-hadoop2.10.1-java8
    container_name: nodemanager
    restart: always
    environment:
      SERVICE_PRECONDITION: "namenode:9000 namenode:50070 datanode:50075 resourcemanager:8088"
    env_file:
      - ./hadoop.env
    ports:
      - 8042:8042
  
  historyserver:
    image: bataeves/hadoop-historyserver:2.0.0-hadoop2.10.1-java8
    container_name: historyserver
    restart: always
    environment:
      SERVICE_PRECONDITION: "namenode:9000 namenode:50070 datanode:50075 resourcemanager:8088"
    volumes:
      - hadoop_historyserver:/hadoop/yarn/timeline
    env_file:
      - ./hadoop.env
    ports:
      - 8188:8188
  
volumes:
  hadoop_namenode:
  hadoop_datanode:
  hadoop_historyserver:
